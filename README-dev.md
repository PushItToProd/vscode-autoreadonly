# Auto-Readonly dev notes

## `pnpm` targets

- `pnpm run vsix` - build a .vsix package
- `pnpm run vsix-install` - install the built .vsix package in VS Code
- `pnpm run vsix-uninstall` - uninstall the extension from VS Code
